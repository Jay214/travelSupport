const config = {
    //启动端口
    port: 80,

    //数据库配置
    database: {
        DATABASE: 'travel',
        USERNAME: 'root',
        PASSWORD: '123456',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;  